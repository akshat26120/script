import React from "react"
import  ReactDOM  from "react-dom/client"

const nest = React.createElement("h1",{},"Rendered Successfully")
const root = ReactDOM.createRoot(document.getElementById("root"))
root.render(nest)
